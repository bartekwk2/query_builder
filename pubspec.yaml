name: query_builder
description: A starting point for Dart libraries or applications.
version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: ">=2.14.0 <4.0.0"

dependencies:
  dart_style: ^3.1.0
  petitparser: ^6.1.0
  oxidized: ">=6.2.0"

dev_dependencies:
  lint: ^2.8.0
  test: ^1.26.2
